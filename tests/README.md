# tests — smoke, logic, stress

Chạy từ gốc repo:

    python3 -m unittest discover -s tests -v

Không cần pip. Fail = workspace lệch ma trận / thiếu file / rào setup.
Sửa file, chạy lại. Nick thật: docs/04-kich-ban-thu.md (smoke tay).
