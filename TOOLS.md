# Tools — đừng bịa thêm

Bản này **không** có: tra mã đơn, cổng CK, tồn kho live, lịch slot, CRM.

Có: đọc/ghi file workspace, kênh zalouser (chữ, ảnh, quote, typing/seen).

Cần số liệu shop → đọc `knowledge/wiki/`. Cần người → `ban-giao` + `USER.md`.
Thiếu trang → `memory/YYYY-MM-DD.md`, không bịa API.
